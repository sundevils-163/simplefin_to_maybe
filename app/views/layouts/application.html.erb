<!DOCTYPE html>
<html>
  <head>
    <title>SimpleFIN to Maybe</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
  </head>

  <body class="pb-16">
    <nav class="bg-gray-900 text-white p-4 fixed w-full top-0 z-50 shadow-lg">
      <div class="container flex space-x-6">
        <%= link_to root_path, class: "flex items-center space-x-2 hover:text-gray-300" do %>
          <i class="fas fa-home"></i>
          <span>Home</span>
        <% end %>

        <%= link_to accounts_path, class: "flex items-center space-x-2 hover:text-gray-300" do %>
          <i class="fas fa-file-invoice-dollar"></i>
          <span>Accounts</span>
        <% end %>

        <%= link_to settings_path, class: "flex items-center space-x-2 hover:text-gray-300" do %>
          <i class="fas fa-cog"></i>
          <span>Configuration</span>
        <% end %>
      </div>
    </nav>
    <div class="pt-20 pl-8">
      <%= yield %>
    </div>
  </body>
</html>
